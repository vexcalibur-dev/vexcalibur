version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.13"
  jobs:
    pre_build:
      - python -m pip install poetry==2.4.1
      - POETRY_VIRTUALENVS_CREATE=false poetry install --extras docs

sphinx:
  configuration: docs/conf.py
  fail_on_warning: true
